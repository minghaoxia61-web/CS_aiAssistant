# CS_Assistant 项目简报

> 更新日期：2026-08-21

## 1. 项目定位

CS_Assistant 是一个面向计算机专业学习的本地优先 AI 复习系统。用户导入 PDF/DOCX/PPTX/Markdown 课件后，可以进行有证据的问答、自适应测验、错题复习、长期知识追踪和主动学习干预。

## 2. 优化前后

| 维度 | 原项目 | 优化后 |
|---|---|---|
| RAG | BM25 + 向量混合检索，主要依赖自动生成评测 | 16 条人工金标集、BM25/N-gram/混合消融、Hit@K/MRR 回归门禁 |
| 学生模型 | 近期加权正确率和规则掌握度 | BKT 掌握概率、下一题预测、Brier/Log Loss/ECE 与经验基线对照 |
| Agent | 规则触发的通知和页面跳转 | 可持久的 7 步状态机，可观察证据、诊断、干预、验证、更新与调度 |
| 安全 | 当前代码已改用环境变量 | 当前树密钥扫描纳入 CI，提供不打印密钥的 Git 历史审计 |
| 演示 | 依赖 4 份静态 PDF，缺失时资料失败 | PDF 解析失败时自动回退到四份内置课件，闭环集成测试保证可演示 |
| 工程 | 单元测试、TypeScript、Lint、构建 | 35 项测试 + RAG Benchmark + 密钥扫描 + 体积预算 + 生产服务冒烟测试 |

## 3. 核心技术

- React 18、TypeScript、Vite、Zustand、IndexedDB、Web Worker。
- 浏览器本地 Transformers.js 向量化，BM25 + N-gram + 向量混合检索。
- PDF/DOCX/PPTX 解析、页码/幻灯片引用、无证据拒答。
- Bayesian Knowledge Tracing、间隔复习、错因分类。
- 可观察 Agent 状态机与安全阻断。
- Express 模型代理、Supabase 可选同步、PWA/Electron/Web 多端。

## 4. 可复现结果

- RAG v1 金标集：16 条人工标注问题。当前内置语料上 BM25 和词法混合检索 Hit@3/MRR 为 100%；该结果只用于小型语料回归，不代表通用泛化能力。
- 自动化测试：35 项通过。
- 质量门禁：TypeScript、ESLint、生产构建、密钥扫描、构建体积和生产路由冒烟测试。

## 5. 简历写法

**项目名：CS_Assistant — 可验证的自适应 AI 学习系统**

- 独立设计 React + TypeScript 本地优先学习系统，在浏览器中完成课件解析、向量化、IndexedDB 持久化与 Web Worker 并行计算。
- 实现 BM25/N-gram/向量混合 RAG 与页码级引用，构建 16 条人工金标问题和消融评测，使用 Hit@K/MRR 回归防止检索质量退化。
- 实现 BKT 知识追踪与严格时序预测，用 Brier Score、Log Loss 和 ECE 对比经验基线，驱动下一题难度与复习计划。
- 构建 observe→diagnose→ground→intervene→verify→update→schedule 可观察 Agent，实现无证据阻断和未验证不更新掌握度的安全约束。
- 建立 35 项自动化测试及 CI 质量门禁，覆盖 RAG、BKT、Agent、密钥扫描、体积预算和生产冒烟测试。

## 6. 客观评价

作为本科生/校招个人项目，综合完成度约 **8/10**，明显高于普通的“调用 LLM API + 聊天页面”项目。优势是本地端 RAG、可复现评测、知识追踪、Agent 闭环和工程门禁形成了完整技术叙事。

局限也很明确：

- 金标集只有 16 条，且内置语料较小，需要更多困难样本、跨课件问题和应拒答问题。
- BKT 参数目前是可配置固定值，尚未用大规模真实学生数据进行拟合。
- 没有真实用户对照实验，因此不能声称已提高学习成绩或记忆保留率。
- Git 历史审计命中 5 个旧提交，必须确认相关旧 Key 已在服务商后台撤销；重写历史需要与协作者单独协调。

面试时建议重点讲 RAG 消融评测、BKT 无泄漏时序预测和 Agent 为什么必须“验证后才更新”，少花时间罗列页面数量。
